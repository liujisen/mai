# A股全市场筛选指南

## 📊 当前状态

程序现在支持筛选**全部A股**（约5000只股票），包括：
- 🔵 **沪市主板**：600/601/603开头
- 🟢 **科创板**：688开头
- 🔴 **深市主板**：000/001开头
- 🟡 **中小板**：002/003开头
- 🟠 **创业板**：300开头

## 🚀 快速开始

### 运行全市场筛选

```bash
cd /Users/insilicomedicine/demo/A
python3 run_mai_screener.py
```

**⚠️ 注意**：
- 筛选全部A股需要**较长时间**（约1-3小时，取决于网络和延迟设置）
- 建议先做小规模测试

## 📝 三种获取股票列表的方式

程序会自动按顺序尝试以下方式：

### 方式1: 东方财富API（首选）
- 优点：数据最新、最准确
- 缺点：连接不稳定时会失败
- 状态：自动尝试

### 方式2: akshare库（推荐安装）
- 优点：稳定、数据完整
- 缺点：需要安装额外的库
- 安装方法：

```bash
pip3 install akshare --user
```

安装后再次运行即可自动使用akshare获取数据。

### 方式3: 代码范围生成（当前自动使用）
- 优点：不需要网络连接
- 缺点：包含未上市代码（约10000个潜在代码）
- 说明：筛选时会自动跳过无效股票

## ⏱️ 运行时间估算

根据不同的配置：

| 股票数量 | 延迟设置 | 预计时间 | 说明 |
|---------|---------|---------|------|
| 50只 | 0.1秒 | 1分钟 | 测试用（预定义列表） |
| 5000只 | 0.1秒 | 8-10分钟 | 实际A股数量 |
| 10000只 | 0.1秒 | 16-20分钟 | 代码范围（含无效） |
| 5000只 | 0.05秒 | 4-5分钟 | 加快速度（可能被限流） |

## 🎯 优化建议

### 1. 安装akshare（强烈推荐）

```bash
# 安装akshare
pip3 install akshare --user

# 验证安装
python3 -c "import akshare; print('akshare安装成功')"
```

安装后程序会自动使用akshare获取准确的股票列表（约5000只）。

### 2. 调整请求延迟

编辑 `stock_screener_mai.py` 中的延迟参数：

```python
# 在 screen_stocks_by_mai_signal 函数中
delay=0.1  # 默认100ms，可改为0.05（50ms）来加快速度
```

**注意**：延迟太小可能导致请求被限制。

### 3. 先做小规模测试

创建测试脚本测试少量股票：

```python
# test_small_batch.py
from stock_screener_mai import main

# 只测试前100只股票
import stock_screener_mai
original_get_list = stock_screener_mai.get_stock_list

def get_limited_list():
    stocks = original_get_list()
    return stocks[:100]  # 只返回前100只

stock_screener_mai.get_stock_list = get_limited_list

# 运行筛选
main(recent_days=3, target_signals=['放量启动', '底背离'], match_mode='OR')
```

## 📊 结果示例

全市场筛选可能找到的股票数量（参考）：

| 筛选条件 | 预计结果数 | 说明 |
|---------|-----------|------|
| 放量启动（OR） | 20-50只 | 近期金叉+放量的股票 |
| 底背离（OR） | 10-30只 | 近期出现底部背离 |
| 放量启动或底背离（OR） | 30-80只 | 满足任一条件 |
| 放量启动且底背离（AND） | 0-5只 | 同时满足（罕见） |
| 共振机会 | 5-15只 | 强烈买入信号 |

## 💡 使用技巧

### 技巧1：分批筛选

如果时间紧迫，可以分批筛选：

1. **第一批：大盘股**（预定义的50只）
2. **第二批：沪市主板**（修改代码只获取沪市）
3. **第三批：深市**（修改代码只获取深市）

### 技巧2：使用多线程（高级）

修改代码添加多线程可以大幅提升速度（需要编程知识）。

### 技巧3：每日定时运行

使用cron或Task Scheduler设置每日自动运行，节省手动操作时间。

## ⚙️ 当前配置

查看 `run_mai_screener.py`：

```python
RECENT_DAYS = 3      # 最近3天
TARGET_SIGNALS = ['放量启动', '底背离']
MATCH_MODE = 'OR'    # 满足任意一个
```

## 🔍 监控进度

运行时会显示进度：

```
进度: 100/5000 (2.0%)
进度: 200/5000 (4.0%)
...
✓ 发现: 万科A(000002) - 放量启动 ⭐ 一般 [01-29]
...
进度: 5000/5000 (100.0%)

筛选完成！共找到 45 只符合条件的股票
```

## 📱 后台运行

如果需要长时间运行，可以使用：

```bash
# Linux/Mac后台运行
nohup python3 run_mai_screener.py > output.log 2>&1 &

# 查看进度
tail -f output.log
```

## ⚠️ 注意事项

1. **网络稳定性**：长时间运行需要稳定的网络
2. **API限流**：请求过快可能被限制
3. **数据准确性**：建议安装akshare获取准确的股票列表
4. **系统资源**：处理大量数据需要足够内存
5. **交易时间**：非交易时段数据可能不是最新的

## 🆘 常见问题

### Q1: 运行时间太长怎么办？
A: 安装akshare减少无效股票，或调小延迟参数

### Q2: 如何只筛选特定板块？
A: 修改 `get_stock_list()` 函数，只返回特定板块的代码

### Q3: 能否并行处理？
A: 需要修改代码添加多线程/多进程支持

### Q4: 结果为0正常吗？
A: 正常，说明市场近期没有符合条件的股票

## 📈 下一步

1. **安装akshare**：获取准确的股票列表
2. **调整参数**：根据需求修改筛选条件
3. **定期运行**：建立每日筛选习惯
4. **结合分析**：将筛选结果与其他分析方法结合

## 免责声明

本工具仅供学习研究使用，不构成投资建议。投资有风险，入市需谨慎。
