# A股Mai指标买入信号筛选器 v3.0

根据Mai技术指标筛选出现买入信号的A股股票。

## 🎯 当前配置

**筛选条件**：最近3天内出现 **放量启动** 或 **底背离**

- 时间范围：最近3天
- 匹配模式：OR（满足任意一个即可）
- 目标信号：['放量启动', '底背离']

## 🚀 快速开始

### 运行筛选器

```bash
cd /Users/insilicomedicine/demo/A
python3 run_mai_screener.py
```

## ⚙️ 自定义配置

编辑 `run_mai_screener.py` 文件：

```python
# 时间范围
RECENT_DAYS = 3      # 1=只看今天, 3=最近3天, 5=最近5天, 10=最近10天

# 目标信号
TARGET_SIGNALS = ['放量启动', '底背离']

# 匹配模式
MATCH_MODE = 'OR'    # 'OR' = 满足任意一个即可
                     # 'AND' = 必须同时满足
```

## 📊 Mai指标买入信号说明

### 主要买入信号

| 信号名称 | 说明 | 强度 | 适用场景 |
|---------|------|------|---------|
| **放量启动** | EMA6上穿EMA18（金叉）+ 放量 | ⭐ 一般 | 一浪进场，趋势启动 |
| **底背离** | 价格创新低但MACD不创新低 | ⭐⭐ 较强 | 反转信号，底部机会 |
| **二浪回踩** | 上升趋势中回踩EMA6支撑 | ⭐ 一般 | 二浪加仓，趋势延续 |
| **共振机会** | 谷值 + 底背离（双重共振） | ⭐⭐⭐ 强烈 | 强烈买入，最佳进场点 |

### 信号组合强度

- ⭐⭐⭐ **强烈**：共振机会、底背离+其他信号
- ⭐⭐ **较强**：两个信号组合
- ⭐ **一般**：单一信号

## 📝 常用配置示例

### 示例1：最近3天 放量启动 或 底背离（当前配置）

```python
RECENT_DAYS = 3
TARGET_SIGNALS = ['放量启动', '底背离']
MATCH_MODE = 'OR'
```

**适用场景**：寻找最新的进场机会

### 示例2：最近5天 放量启动 且 底背离（同时满足）

```python
RECENT_DAYS = 5
TARGET_SIGNALS = ['放量启动', '底背离']
MATCH_MODE = 'AND'
```

**适用场景**：寻找高质量买点（条件更严格）

### 示例3：最近10天 只看共振机会

```python
RECENT_DAYS = 10
TARGET_SIGNALS = ['共振机会']
MATCH_MODE = 'OR'
```

**适用场景**：寻找最强烈的买入信号

### 示例4：最近7天 任意买入信号

```python
RECENT_DAYS = 7
TARGET_SIGNALS = ['放量启动', '底背离', '二浪回踩', '共振机会']
MATCH_MODE = 'OR'
```

**适用场景**：广撒网，寻找所有可能的机会

## 📈 输出结果

筛选结果包含以下信息：

- **股票代码/名称**：基本信息
- **最新价**：当前价格
- **EMA6/EMA18**：短期/长期趋势线
- **止损线**：风险控制参考
- **买入信号**：触发的信号类型
- **信号强度**：⭐/⭐⭐/⭐⭐⭐
- **信号日期**：信号出现的日期
- **趋势**：当前趋势方向
- **更新日期**：数据更新日期

## 💾 保存位置

筛选结果自动保存到：
- `mai_buy_signals_YYYYMMDD.csv` （项目根目录）
- `results/mai_buy_signals_YYYYMMDD.csv` （results目录）

## ⚠️ 注意事项

1. **信号稀缺性**：
   - "放量启动 + 底背离"同时出现非常罕见
   - 建议使用 OR 模式（满足任意一个）
   - 或增加时间范围（如10天）

2. **市场环境**：
   - 震荡市或下行市，买入信号较少
   - 上行趋势中，信号更频繁

3. **数据来源**：
   - 使用Ashare库获取实时数据
   - 数据来自新浪/腾讯财经API
   - 非交易日可能无最新数据

## 🔧 其他相关脚本

- `stock_screener_mai.py` - 核心筛选逻辑
- `test_mai_signals.py` - 测试单个股票的信号
- `check_individual_signals.py` - 检查信号出现频率
- `mai_indicator.py` - Mai指标计算库

## 💡 使用建议

1. **首次使用**：先用 OR 模式查看各种信号的情况
2. **调整参数**：根据结果数量调整时间范围
3. **组合筛选**：可以多次运行，对比不同配置的结果
4. **人工复核**：筛选结果仅供参考，需结合其他分析

## 📊 效果评估

建议记录以下信息评估效果：
- 信号出现后的股价表现
- 不同信号组合的成功率
- 最佳进场时机（信号当天 vs 次日）

## 免责声明

本工具仅用于技术分析学习和研究，不构成任何投资建议。投资有风险，入市需谨慎。
