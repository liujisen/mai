# Mai指标筛选器 - 快速开始

## ✅ 已完成功能

现在支持筛选**全部A股**（主板、创业板、科创板），大约5000-10000只股票。

## 🚀 三种运行方式

### 1. 小规模测试（推荐首次使用）

测试前100只股票，快速验证功能（约2分钟）：

```bash
cd /Users/insilicomedicine/demo/A
python3 run_mai_screener_small.py
```

✅ **测试成功**：刚才运行找到了2只符合条件的股票！

### 2. 全市场筛选（完整版）

筛选全部A股，需要较长时间（约20分钟-1小时）：

```bash
python3 run_mai_screener.py
```

⏱️ **预计时间**：
- 10000只潜在代码 × 0.1秒延迟 = 约16-20分钟
- 实际有效股票约5000只
- 如果安装akshare，只需8-10分钟

### 3. 精选股票池（快速版）

使用48只精选活跃股（约1分钟）：

在 `run_mai_screener.py` 开头添加：

```python
import stock_screener_mai
stock_screener_mai.get_stock_list = lambda: [
    # 返回预定义的48只精选股票
    ... 
]
```

## 📊 当前配置

```python
RECENT_DAYS = 3                          # 最近3天
TARGET_SIGNALS = ['放量启动', '底背离']  # 目标信号
MATCH_MODE = 'OR'                        # 满足任意一个
```

## 🎯 优化建议

### 🔥 强烈推荐：安装akshare

```bash
pip3 install akshare --user
```

**优点**：
- 获取准确的股票列表（约5000只实际上市股票）
- 不包含未上市代码，节省时间
- 运行时间减少50%

**验证安装**：

```bash
python3 -c "import akshare; print('akshare安装成功！')"
```

### ⚡ 加快速度

编辑 `stock_screener_mai.py`，修改延迟参数：

```python
# 在 screen_stocks_by_mai_signal 函数调用中
delay=0.05  # 从0.1改为0.05，速度提升1倍
```

**注意**：延迟太小可能被API限流。

## 📈 测试结果

**小规模测试（前100只）**：
- 运行时间：约21秒
- 找到股票：2只
- 信号类型：放量启动

**预估全市场结果**（基于比例推算）：
- 10000只代码 → 约200只符合条件
- 实际有效股票约5000只 → 约100只符合条件

## 🔍 查看结果

结果保存在：
- `mai_buy_signals_YYYYMMDD.csv`（根目录）
- `results/mai_buy_signals_YYYYMMDD.csv`（results目录）

## 📋 三种获取股票方式

程序会自动按顺序尝试：

1. **东方财富API**（最优）
   - 状态：当前连接不稳定 ❌
   - 需要：稳定网络

2. **akshare库**（推荐）
   - 状态：未安装 ⚠️
   - 安装：`pip3 install akshare --user`
   - 安装后自动使用 ✅

3. **代码范围生成**（当前使用）
   - 状态：自动启用 ✅
   - 说明：包含约10000个潜在代码
   - 限制：包含未上市代码（会自动跳过）

## ⚙️ 修改配置

编辑 `run_mai_screener.py`：

```python
# 时间范围
RECENT_DAYS = 5      # 改为5天

# 信号组合
TARGET_SIGNALS = ['共振机会']  # 只看最强信号

# 匹配模式
MATCH_MODE = 'AND'   # 必须同时满足
```

## 💡 使用场景

| 场景 | 推荐方式 | 运行时间 |
|-----|---------|---------|
| 🧪 **首次测试** | small版本 | 2分钟 |
| 🔍 **快速筛选** | 精选48只 | 1分钟 |
| 📊 **完整扫描** | 全市场（安装akshare后） | 10分钟 |
| 🎯 **深度挖掘** | 全市场（代码范围） | 20分钟 |

## 📞 常见问题

### Q: 为什么是10000只而不是5000只？
A: 因为使用代码范围生成，包含所有可能的代码（含未上市）。安装akshare后会精确到5000只。

### Q: 筛选时间太长怎么办？
A: 
1. 安装akshare（首选）
2. 减小延迟（0.05秒）
3. 使用small版本测试
4. 分批筛选不同板块

### Q: 如何只筛选创业板？
A: 修改 `get_stock_list()` 函数，只返回300开头的代码。

### Q: 能否后台运行？
A: 可以，使用 `nohup python3 run_mai_screener.py &`

## 🎉 下一步

1. ✅ 先运行小规模测试（已完成）
2. 📦 安装akshare提升性能（推荐）
3. 🚀 运行全市场筛选
4. 📊 分析筛选结果
5. 🔄 定期运行（每日/每周）

## 📝 示例命令

```bash
# 1. 小规模测试（2分钟）
python3 run_mai_screener_small.py

# 2. 安装akshare
pip3 install akshare --user

# 3. 全市场筛选（10-20分钟）
python3 run_mai_screener.py

# 4. 查看结果
cat mai_buy_signals_*.csv
```

## 🎯 预期效果

根据当前市场情况，全市场筛选预计找到：
- **放量启动**：30-60只
- **底背离**：20-40只
- **放量启动 或 底背离**：50-100只
- **共振机会**：5-15只

## ⚠️ 重要提示

- 💰 筛选结果仅供参考，不构成投资建议
- ⏰ 非交易日数据可能不是最新
- 🔄 建议交易日盘后运行
- 📊 结合其他分析方法使用

准备好了吗？开始筛选吧！🚀
